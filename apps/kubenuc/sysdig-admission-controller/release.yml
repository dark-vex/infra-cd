---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: sysdig-admission-controller
  namespace: sysdig-admission-controller
spec:
  chart:
    spec:
      chart: admission-controller
      version: ">=0.6.24"
  values:
    sysdig:
      url: eu1.app.sysdig.com
    clusterName: ranchernuc
