# CI-only placeholder for the k8s-vms-daniele semaphore HelmRelease's
# valuesFrom lookup (clusters/k8s-vms-daniele/apps/semaphore/manifests/release.yml).
# Overwrites the real OnePasswordItem manifest (semaphore-secret.yml) in
# the ephemeral runner workspace only - never committed.
apiVersion: v1
kind: Secret
metadata:
  name: semaphore-credentials
  namespace: semaphore
stringData:
  admin-username: ci-placeholder-admin
  admin-password: ci-placeholder-password
  admin-email: ci-placeholder@ci.invalid
