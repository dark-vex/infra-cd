---
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: tunnel-token
  namespace: cloudflare
spec:
  # Create this item in 1Password with a field:
  #   token - Tunnel token for the kubenuc cloudflared tunnel (UUID
  #           03bafc6f-094c-46d2-9ac0-1663a74f84ee), obtained via
  #           `cloudflared tunnel token <tunnel-id>` or the Zero Trust
  #           dashboard (Networks -> Tunnels -> select tunnel -> Configure).
  itemPath: "vaults/k8s_secrets/items/cloudflare-tunnel-kubenuc"
