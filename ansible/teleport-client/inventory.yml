all:
  children:
    teleport_client:
      hosts:
        mon-bgy:
          # ansible_host is in host_vars/mon-bgy.sops.yaml (SOPS-encrypted —
          # see ansible/CLAUDE.md)
          teleport_labels:
            site: bgy
            role: monitoring-node

        mon-lug:
          # ansible_host is in host_vars/mon-lug.sops.yaml (SOPS-encrypted —
          # see ansible/CLAUDE.md)
          teleport_labels:
            site: lug
            role: monitoring-node

        mon-mxp:
          # ansible_host is in host_vars/mon-mxp.sops.yaml (SOPS-encrypted —
          # see ansible/CLAUDE.md)
          teleport_labels:
            site: mxp
            role: monitoring-node
